# Workflows — Plateforme-AFD

Tables : `workflow_definitions`, `workflow_states`, `workflow_transitions`, `approval_requests`, `approval_decisions`, `workflow_history`.

Service : `src/features/workflows/services/workflow.service.ts`

Transitions invalides rejetées côté serveur.

