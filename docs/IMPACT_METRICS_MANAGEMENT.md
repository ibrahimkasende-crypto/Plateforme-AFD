# Gestion des chiffres d’impact

Table `chiffres_impact` : key, label, value, unit, suffix, validated, validation_source, reference_period…

Règle : une valeur non validée (`validated = false`) n’est pas affichée comme officielle.

Studio : `/admin/publications/notre-impact`
